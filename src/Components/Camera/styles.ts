import styled from "styled-components/native";
import { Camera } from "expo-camera";

export const Container = styled.View`
  flex: 1;
`;
export const CameraCustomStyle = styled(Camera)`
  flex: 1;
`;
